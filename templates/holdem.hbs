<div class="hhv-hand {{info.anyActivity}} {{info.playerActivity}}">
  <div class="hhv-header">
    <span class="hhv-bb-sb-ante-max">
      ({{ info.bb }}/{{ info.sb }})
      {{# if info.ante }}
        ({{ info.ante }})
      {{/if}}
      [{{ table.maxseats }}]
    </span>
    <span class="hhv-board">
    {{# if renderedBoard }}
      {{# if renderedBoard.card1 }} {{{ renderedBoard.card1 }}} {{/if}}
      {{# if renderedBoard.card2 }} {{{ renderedBoard.card2 }}} {{/if}}
      {{# if renderedBoard.card3 }} {{{ renderedBoard.card3 }}} {{/if}}
      {{# if renderedBoard.card4 }} {{{ renderedBoard.card4 }}} {{/if}}
      {{# if renderedBoard.card5 }} {{{ renderedBoard.card5 }}} {{/if}}
    {{ else }}
      &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;
    {{/if}}
    </span>
    {{# if info.day }}
    <span class="hhv-date">
      {{#twodigits}}{{info.month}}{{/twodigits}}/{{#twodigits}}{{info.day}}{{/twodigits}}/{{info.year}}
    </span>
    {{/if}}
    <span class="hhv-date">
      {{#twodigits}}{{info.hour}}{{/twodigits}}:{{#twodigits}}{{info.min}}{{/twodigits}}:{{#twodigits}}{{info.sec}}{{/twodigits}}
    </span>
    <span class="hhv-gameinfo">
      {{# ifvalue info.gametype value="tournament"}} T: {{/ifvalue}}
      {{ info.gameno }}
      G: {{ info.handid }}
    </span>
  </div>
  <div class="hhv-table">
    <table>
      <thead>
      <tr>
        <th>Pos</th>
        <th>Name</th>
        <th>Cards</th>
        <th>M</th>
        <th>Preflop</th>
        <th>Flop</th>
        <th>Turn</th>
        <th>River</th>
      </tr>
      </thead>
      <tbody>
      {{# each renderedPlayers }}
      <tr class="hhv-player {{playerActivity}}">
        <td>{{ pos }}</td>
        <td>{{ name }}</td>
        <td>{{{ renderedCards.card1 }}}{{{ renderedCards.card2 }}}</td>
        <td>{{ m }}</td>
        <td>{{ preflop }}</td>
        <td>{{ flop }}</td>
        <td>{{ turn }}</td>
        <td>{{ river }}</td>
        <td>{{ showdown }}</td>
      </tr>
      {{/each}}
      </tbody>
    </table>
  </div>
</div>
